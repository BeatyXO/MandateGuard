[project]
name = "mandateguard"
version = "0.1.0"
description = "Semantic capability firewall primitive for autonomous agents on GenLayer"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests/direct"]
addopts = "-q"
